on:
  push:
    branches-ignore:
      - 'citest-*'
    tags-ignore:
      - '**'
  workflow_dispatch:

jobs:
  test:
    uses: cyberrangecz/ci-images/.github/workflows/test.yml@master
    with:
      release_to_s3: false
    secrets: inherit
